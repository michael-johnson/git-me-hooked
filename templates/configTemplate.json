{
    "scripts": {
        "pre-commit": [
            {
                "exec": "echo \"Git Me Hooked is installed, but no scripts are to configured to be executed on pre-commit. Edit git-me-hooked.json to configure your commit hooks.\""
            }
        ]
    }
}